export const metadata = {
    title: 'Extension Callback | Whisk Automator',
    description: 'Connecting your Whisk Automator extension. This will only take a moment.',
    robots: {
        index: false,
        follow: false,
    },
};

export default function ExtensionCallbackLayout({ children }) {
    return <>{children}</>;
}
